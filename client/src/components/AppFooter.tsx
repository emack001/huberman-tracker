/**
 * AppFooter — white-label footer replacing the Perplexity attribution.
 * Shows the app name and a subtle copyright line.
 */
export function AppFooter() {
  return (
    <footer className="w-full py-3 text-center text-[10px] text-muted-foreground/50 select-none">
      JMR Wellness · Huberman Protocol Tracker
    </footer>
  );
}
